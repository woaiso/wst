'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _lodash = require('lodash.compact');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var renderChildToJson = function renderChildToJson(child) {
    if (!child) return;

    if (child.type === 'tag') {
        return {
            type: child.name,
            props: child.attribs,
            children: child.children && child.children.length ? (0, _lodash2.default)(child.children.map(renderChildToJson)) : null,
            $$typeof: Symbol.for('react.test.json')
        };
    } else if (child.type === 'text') {
        return child.data;
    }
};

exports.default = function (wrapper) {
    return renderChildToJson(wrapper.children()[0]);
};