'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toHaveClassName;

var _negateMessage = require('../negateMessage');

var _negateMessage2 = _interopRequireDefault(_negateMessage);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function toHaveClassName(enzymeWrapper, className) {
  var normalizedClassName = className.split(' ').join('.');

  if (normalizedClassName[0] !== '.') {
    normalizedClassName = '.' + normalizedClassName;
  }

  var pass = enzymeWrapper.is(normalizedClassName);

  return {
    pass: pass,
    message: (0, _negateMessage2.default)(pass, 'Expected "' + enzymeWrapper.html() + '" to have className of ' + className + ' but instead found ' + enzymeWrapper.props('className') // eslint-disable-line max-len
    )
  };
} /**
   * This source code is licensed under the MIT-style license found in the
   * LICENSE file in the root directory of this source tree. *
   *
   * @providesModule toHaveClassNameAssertion
   * 
   */

module.exports = exports['default'];