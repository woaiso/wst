'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = toHaveProp;

var _negateMessage = require('../negateMessage');

var _negateMessage2 = _interopRequireDefault(_negateMessage);

var _deepEqualIdent = require('deep-equal-ident');

var _deepEqualIdent2 = _interopRequireDefault(_deepEqualIdent);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * This source code is licensed under the MIT-style license found in the
 * LICENSE file in the root directory of this source tree. *
 *
 * @providesModule toHavePropAssertion
 * 
 */

function toHaveProp(enzymeWrapper, propKey, propValue) {
  var props = enzymeWrapper.props();

  // error if the prop doesnt exist
  if (!props.hasOwnProperty(propKey)) {
    return {
      pass: false,
      message: 'Expected wrapper to have prop "' + propKey + '"'
    };
  }

  // key exists given above check, and we're not validating over values,
  // so its always true
  if (propValue === undefined) {
    return {
      pass: true,
      message: ''
    };
  }

  var pass = (0, _deepEqualIdent2.default)(props[propKey], propValue);

  return {
    pass: pass,
    message: (0, _negateMessage2.default)(pass, 'Expected wrappers prop values to match for key "' + propKey + '":\n        Actual: ' + JSON.stringify(props[propKey]) + '\n        Expected: ' + JSON.stringify(propValue) + '\n      ')
  };
}
module.exports = exports['default'];